"""rembg cutout adapter (birefnet-general). M1 (#8). Not implemented yet."""

from __future__ import annotations

from pathlib import Path


def cut(gpose_path: Path, out_path: Path) -> Path:
    """Run background removal on a gpose screenshot."""
    raise NotImplementedError("cutout is implemented in M1")
